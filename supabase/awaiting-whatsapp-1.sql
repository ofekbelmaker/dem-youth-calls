-- =====================================================================
--  "ממתין לתשובה בוואטסאפ" — שלב 1 מתוך 2
--
--  רק הוספת הערך ל-enum. זה הכל.
--
--  למה קובץ נפרד: Postgres אוסר להשתמש בערך enum חדש באותה
--  טרנזקציה שבה הוא נוצר, וה-SQL Editor מריץ כל הרצה כטרנזקציה
--  אחת. הרצת שלב 2 יחד עם זה נכשלת ב-55P04 ומגלגלת הכל אחורה.
--
--  להריץ, לוודא Success, ורק אז לפתוח את awaiting-whatsapp-2.sql
-- =====================================================================

alter type contact_outcome add value if not exists 'awaiting_whatsapp';
